/* =========================================================
   KORDIVA — ORDER TRACKING
========================================================= */

document.addEventListener(
    "DOMContentLoaded",
    function () {

        const orderInput =
            document.getElementById("order_id");


        if (orderInput) {

            orderInput.focus();

        }

    }
);